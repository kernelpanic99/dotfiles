[opener]
mpv = [{ run = 'mpv --force-window "$@"', orphan = true, for = "unix" }]
mdfried = [
  { run = "mdfried %s", block = true, for = "unix", desc = "View markdown with mdfried" },
]
nvim = [
  { run = "nvim %s", block = true, for = "unix", desc = "Edit with neovim" },
]
xournalpp = [
  { run = "xournalpp %s", block = false, orphan = true, for = "unix", desc = "Open with Xournal++" },
]

zathura = [
  { run = "zathura %s", block = false, orphan = true, for = "unix", desc = "Open with Zathura" },
]

[open]
prepend_rules = [
  { url = "*.md", use = [
    "nvim",
    "mdfried",
  ] },
  { mime = "video/*", use = [
    "mpv",
    "reveal",
  ] },
  { mime = "application/{,g}zip", use = [
    "extract",
  ] },
  { mime = "application/x-{tar,bzip*,7z-compressed,xz,rar}", use = [
    "extract",
  ] },
  { url = "*.pdf", use = [
    "xournalpp",
  ] },
]

[[plugin.prepend_previewers]]
mime = "application/{*zip,tar,bzip2,7z*,rar,xz,zstd,java-archive}"
run = "ouch"
